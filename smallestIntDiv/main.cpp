#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int smallestRepunitDivByK(int k) {
        //find n which has same prime numbers than k and contains only 1
        if (k % 2 == 0) return -1;
        //split k into prime numbers
        bool foundMultiple = false;
        int i = 0;
        int n = 0;
        while (!foundMultiple) {
            n += pow(10, i);


        }
    }
};;

int main() {


    return 0;
}